"""Home Activity Lecture 3 App 4    Developed by JC     01.15.25

App 4:
A score is on grade basis or not.
User enters 1 for grade basis, 0 for not graded.
User enters score (0..100) integer.
grade basis:  >= 80  A  else B
not grade basis  > 40 P(ass)  else F(ail)
"""

score = int(input('Enter Score >>'))
graded = int(input('Enter 1 for grade basis, 0 for pass/fail >>'))

if graded == 1:
    #apply A B rule
    if score >= 80:
        letter_grade = 'A'
    else:
        letter_grade = 'B'
else:
    #apply P F rule
    if score > 40:
        letter_grade = 'P'
    else:
        letter_grade = 'F' 

print(letter_grade)